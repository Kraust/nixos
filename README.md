# nixos

```bash
$ git clone git@github.com:Kraust/nixos.git
$ sudo ln -sf ~/git/nixos/configuration.nix /etc/nixos/
$ sudo ln -sf ~/git/nixos/tiny-stars.nix /etc/nixos/local-configuration.nix
$ sudo ln -sf ~/git/nixos/plasma.nix /etc/nixos/desktop.nix
```
